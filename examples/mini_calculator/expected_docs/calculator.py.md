# mini_calculator

## Модуль `calculator.py`

Simple calculator module.

## Классы

### `class Calculator(object)`

Performs basic arithmetic operations.

## Функции

### `def __init__(precision: int = 2) -> None`

Выполняет операцию `__init__`.

**Параметры:**
- `precision` (`int`) — параметр функции

**Возвращаемое значение:** `None` — результат операции

### `def format_result(value: float) -> str`

Выполняет операцию `format_result`.

**Параметры:**
- `value` (`float`) — параметр функции

**Возвращаемое значение:** `str` — результат операции

### `def multiply(x: int, y: int) -> int`

Выполняет операцию `multiply`.

**Параметры:**
- `x` (`int`) — параметр функции
- `y` (`int`) — параметр функции

**Возвращаемое значение:** `int` — результат операции

### `def add(a: float, b: float) -> float`

Add two numbers.

**Параметры:**
- `a` (`float`) — параметр функции
- `b` (`float`) — параметр функции

**Возвращаемое значение:** `float` — результат операции

### `def divide(a: float, b: float) -> Optional[float]`

Выполняет операцию `divide`.

**Параметры:**
- `a` (`float`) — параметр функции
- `b` (`float`) — параметр функции

**Возвращаемое значение:** `Optional[float]` — результат операции
