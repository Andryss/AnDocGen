# Модуль `storage.py`


Модуль хранения данных в памяти.

**Содержание:**

- [Классы](#классы)
- [Функции](#функции)

## Классы

### `class InMemoryStorage`

Класс для простого хранения данных в памяти для демонстрационных целей.

#### Методы

##### `__init__() -> None`

Инициализирует объект класса InMemoryStorage.

**Возвращаемое значение:**

- `None` — None

##### `create(payload: dict[str, Any]) -> int`

Создаёт новый элемент в хранилище и возвращает его идентификатор.

**Параметры:**

- `payload` (`dict[str, Any]`) — словарь с данными для сохранения

**Возвращаемое значение:**

- `int` — идентификатор созданного элемента

##### `get(item_id: int) -> dict[str, Any] | None`

Возвращает словарь с данными элемента по его идентификатору или None, если элемент не найден.

**Параметры:**

- `item_id` (`int`) — идентификатор элемента

**Возвращаемое значение:**

- `dict[str, Any] | None` — словарь с данными элемента или None, если элемент не найден

## Функции

### `def normalize_email(email: str) -> str`

Приводит email к нижнему регистру и удаляет начальные и конечные пробелы для хранения.

**Параметры:**

- `email` (`str`) — email для нормализации

**Возвращаемое значение:**

- `str` — нормализованный email

**Примеры:**

[{"name": "normalize_email", "arguments": {"email": "  user@example.com  "}}]


---

[← Индекс](README.md)
