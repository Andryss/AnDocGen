# Модуль `plugins/base.py`


Базовый модуль плагинов для мини-библиотеки.

**Содержание:**

- [Классы](#классы)

## Классы

### `class PluginBase(ABC)`

Класс для базовых экспортировальных плагинов.

**Наследование:**

- `ABC`

#### Методы

##### `__init__(*args: Any, **kwargs: Any) -> None`

Инициализирует экземпляр класса PluginBase с параметрами.

**Параметры:**

- `*args` (`Any`) — —
- `**kwargs` (`Any`) — —

**Примеры:**

Инициализировать экземпляр PluginBase с опциями.

```python
plugin = PluginBase(options={'option': 'value'})
```

##### `export(data: dict[str, Any]) -> str`

Преобразует данные в строковое представление.

**Параметры:**

- `data` (`dict[str, Any]`) — данные для преобразования

**Возвращаемое значение:**

- `str` — строковое представление данных

**Примеры:**

Преобразовать словарь в строку.

```python
PluginBase.export({'key': 'value'})
```


---

[← plugins](README.md) | [← К проекту](../README.md)
