# Модуль `models.py`


Модуль с классами данных для отдельных элементов в заказах и самих заказов.

**Содержание:**

- [Классы](#классы)

## Классы

### `class Item`

Класс для представления отдельного элемента в заказе.

**Поля:**

- `sku` (`str`) — —
- `title` (`str`) — —
- `price` (`float`) — —
- `quantity` (`int`) — —

### `class Order`

Класс для представления заказов.

**Поля:**

- `order_id` (`int`) — —
- `customer` (`str`) — —
- `items` (`list[Item]`) — —

#### Методы

##### `total() -> float`

Вычисляет и возвращает общую сумму заказа.

**Возвращаемое значение:**

- `float` — общая сумма заказа

**Примеры:**

Вычислить общую сумму заказа.

```python
order.total()
```


---

[← Индекс](README.md)
